import React from "react";
import { ContentWrapper, RenderAuth } from "components";

const Login = (): React.ReactElement => {
  return (
    <ContentWrapper>
      <RenderAuth />
    </ContentWrapper>
  );
};

export default Login;
